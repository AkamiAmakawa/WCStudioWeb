<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Match history</title>
    <link rel="stylesheet" href="/css/match_history_list.css">
</head>
<body>
   <div id="header">
       <%- include('./partials/navbar.ejs') %>
   </div> 
   <div id="main">
        <div id="left-content"></div>
        <div id="main-content">
            <div id="title">
                <h1>Match history</h3>
            </div>
            <div id="history-list">
                <% if(data.length > 0) { %>
                <ul class="matchlist">
                    <% for(match of data) { %>
                    <li>
                        <div class="match-id">  
                            <a href="/match_detail/<%= match.id %>">BOSS FIGHT #<%=match.id%></a>
                        </div>
                        <div class= "match-details">
                            <p>At: <%= match.createdAt %></p>
                            <a href="/user/<%= match.player1_id %>"><%-match.player1_info.displayName %> </a>
                            <span> AGAINST </span>
                            <a href="/user/<%= match.player2_id %>"><%-match.player2_info.displayName %> </a>
                        </div>
                    </li>
                    <% } %>
                </ul>
                <% }else{ %>
                    <p class="no_data_msg">NO BATTLE YET</p>
                <% } %> 
            </div>
        </div>
        <div id="right-content"></div>
   </div>
   <div id="footer">
       <%- include('./partials/footer.ejs') %>
   </div>
</body>
</html>